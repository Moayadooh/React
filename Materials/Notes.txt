
SPA --> Single Page Application

code in App.js is called JSX

React.StrictMode --> check if there is an errors

Application Structure:
node_modules --> react dependencies
public --> include main page "index.html"

Ctrl+K+C --> comment the code
Ctrl+K+U --> uncomment the code
Ctrl+K+F --> code formatting

sfc + tab

rfce + tab

div.classname + tap

{} --> javascript code written inside curly brazes

if(null) --> return false
if(false && true) --> will evaluate from the left first and will stop(since it's false)

Custom Hook --> function name must start with "use"

Redux DevTools:
https://github.com/zalmoxisus/redux-devtools-extension#usage

fake API for testing and prototyping:
https://jsonplaceholder.typicode.com/
