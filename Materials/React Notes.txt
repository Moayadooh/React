C:\Users\moaya\Documents\React\moayad-blog

SPA --> Single Page Application

code in App.js is called JSX

React.StrictMode --> check if there is an errors

Application Structure:
node_modules --> react dependencies
public --> include main page "index.html"

Commands:
node -v --> display Node JS version
npx create-react-app moayad-blog --> create react project named(moayad-blog)
code . --> open peoject with VS Code

Terminal:
npm run start --> run the application
npm install --> download node_modules folder
npx json-server --watch data/db.json --port 7000 --> specify localhost port to call API (db.json)
Ctrl+C in the terminal then yes --> terminate the connection to localhost port

Ctrl+K+C --> comment the code
Ctrl+K+U --> uncomment the code
Ctrl+K+F --> code formatting

sfc + tab

div.classname + tap

{} --> javascript code written inside curly brazes

if(null) --> return false
if(false && true) --> will evaluate from the left first and will stop(since it's false)
